from collections import OrderedDict

class BaseCollection:

    def __init__():
        models = OrderedDict()
        key = None
        
    def populate(records):
        try:
            if key = None:
                i = 0
                for record in records:
                    self.models[i] = record
            else:
                #TODO
                pass
        except Exception as e:
            print(f"Failed to populate collection: {str(e)}")

